import { test } from '@playwright/test';

test.describe('Agent seed', () => {
  test('seed', async ({ page }) => {
    // Add login or setup steps here when the Playwright Test Agents need a starting page.
  });
});
