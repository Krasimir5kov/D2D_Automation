/**
 * Covers: the Importe "Benutzer" filter dropdown's own content/structure — search input
 * presence/placeholder, header label, and counter badge behavior on selecting an option.
 * Does NOT cover: selecting/applying a value or checking list results — see
 * importeBenutzerFilterApply.spec.ts for that.
 * Trigger: #importedByUser.
 */
import { test } from '@playwright/test';

test.describe.skip('Importe Benutzer Filter Dropdown Content', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
