/**
 * Covers: the Importe "Datei Import" quick filter pill — applying it and verifying
 * results.
 * Trigger: #quick-filter-systemImport-datei-import.
 */
import { test } from '@playwright/test';

test.describe.skip('Importe Datei Import Quick Filter Apply', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
