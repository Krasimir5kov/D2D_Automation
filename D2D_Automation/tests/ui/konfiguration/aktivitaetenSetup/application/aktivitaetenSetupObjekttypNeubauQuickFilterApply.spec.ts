/**
 * Covers: the Aktivitäten Setup "Neubau" objectType quick filter pill — applying it and
 * verifying results against the row's data-object-type="Neubau" attribute.
 * Trigger: #quick-filter-objectType-NEUBAU.
 */
import { test } from '@playwright/test';

test.describe.skip('Konfiguration Aktivitäten Setup Objekttyp Neubau Quick Filter Apply', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
