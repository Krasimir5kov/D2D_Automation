/**
 * Covers: the Baulose search field — entering text and the list updating
 * accordingly.
 */
import { test, expect } from '../../../src/fixtures/baulose.fixture';

test.describe('Baulose Search', () => {
  // TODO: search tests
});
