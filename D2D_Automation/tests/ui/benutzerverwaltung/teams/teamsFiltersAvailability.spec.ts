/**
 * Covers: Teams tab filter triggers — "Organisation" dropdown plus the "ohne Mitglieder"
 * quick-filter pill — visibility and correct title/label.
 * Does NOT cover: selecting/applying a value or checking list results — see
 * teamsOrganisationFilterApply.spec.ts / teamsOhneMitgliederQuickFilterApply.spec.ts for that.
 * Triggers: #organizations, quick-filter-noTeamMembers-no-team-members.
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Teams Filters Availability', () => {
});
