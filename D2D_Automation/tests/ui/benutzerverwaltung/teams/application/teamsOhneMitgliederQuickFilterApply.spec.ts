/**
 * Covers: the Benutzerverwaltung Teams tab "ohne Mitglieder" quick filter pill — applying it
 * and verifying results.
 * Trigger: #quick-filter-noTeamMembers-no-team-members (Teams tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Teams Ohne Mitglieder Quick Filter Apply', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
