/**
 * Covers: the Benutzerverwaltung Users tab "Organisation" filter — applying it and
 * verifying results.
 * Trigger: #organizations (Users tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Users Organisation Filter Apply', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
