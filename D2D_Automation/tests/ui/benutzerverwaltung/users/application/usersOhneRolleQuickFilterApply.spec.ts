/**
 * Covers: the Benutzerverwaltung Users tab "ohne Rolle" quick filter pill — applying it and
 * verifying results.
 * Trigger: #quick-filter-roles-no-role (Users tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Users Ohne Rolle Quick Filter Apply', () => {
});
