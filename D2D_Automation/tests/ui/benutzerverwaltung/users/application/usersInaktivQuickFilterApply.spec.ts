/**
 * Covers: the Benutzerverwaltung Users tab "inaktiv" quick filter pill — applying it and
 * verifying results.
 * Trigger: #quick-filter-activeUser-inactive-users (Users tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Users Inaktiv Quick Filter Apply', { tag: ['@Admin', '@Admin-Regional'] }, () => {
});
