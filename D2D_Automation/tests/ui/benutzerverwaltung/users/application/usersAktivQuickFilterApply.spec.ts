/**
 * Covers: the Benutzerverwaltung Users tab "aktiv" quick filter pill — applying it and
 * verifying results.
 * Trigger: #quick-filter-activeUser-active-users (Users tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Users Aktiv Quick Filter Apply', () => {
});
