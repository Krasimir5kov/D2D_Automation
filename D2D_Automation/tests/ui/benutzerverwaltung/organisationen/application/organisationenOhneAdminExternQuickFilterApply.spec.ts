/**
 * Covers: the Benutzerverwaltung Organisationen tab "ohne Admin extern" quick filter pill —
 * applying it and verifying results.
 * Trigger: #quick-filter-no-admin-extern-no-admin-extern (Organisationen tab).
 */
import { test } from '@playwright/test';

test.describe.skip('Benutzerverwaltung Organisationen Ohne Admin Extern Quick Filter Apply', () => {
});
