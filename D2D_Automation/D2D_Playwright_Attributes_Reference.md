# D2D Frontend — Playwright Attributes Reference
## All QA IDs & Data Attributes — sourced from `src/frontend/shared/testIds/`

> **How to read this file:**
> - `{id}` = dynamic DB id (number or string)
> - `(fn)` = ID is generated by a function — pattern shown as the resolved string
> - All selectors are Playwright-ready
> - `data-*` attributes are set on the same element as `id` unless noted otherwise

---

## 1. Quick Filters — Shared Across All List Pages

**File:** `quickFilter.ids.ts`  
**Element:** `<button>` pill rendered by `QuickFilter.tsx`  
**Built-in attribute:** `aria-pressed="true|false"` (already in the component)

| What | ID pattern | Playwright selector |
|---|---|---|
| Filter container | `quick-filter-{filterId}` | `#quick-filter-objectType` |
| Filter pill | `quick-filter-{filterId}-{choiceId}` | `#quick-filter-objectType-NEUBAU` |
| Filter pill (selected) | — | `#quick-filter-objectType-NEUBAU[aria-pressed="true"]` |
| Clear all filters button | `clear-all-applied-filters-button` | `#clear-all-applied-filters-button` |

**Standard object-type pill IDs (all list pages with type filter):**
```
#quick-filter-objectType-NEUBAU
#quick-filter-objectType-FTTH
#quick-filter-objectType-BESTANDSBAU
```

---

## 2. Baulose — List Page

**File:** `baulose.ids.ts`  
**Element:** `<tr>` via `rowAttributes`

| What | ID pattern | Playwright selector |
|---|---|---|
| Table row | `{id}-{ftthId}` (ftthId optional) | `#123-` or `#123-ftth456` |
| Sales actions link | `sales-actions-link-{type}-{contractSectionId}-{ftthId}` | `#sales-actions-link-neubau-10-` |
| Page navigator | `page-navigator-{type}` | `#page-navigator-neubau` |

---

## 3. Object List — Neubau / FTTH / Bestandsbau

**File:** `objectList.ids.ts`  
**Element:** `<tr>` and its cells via `rowAttributes`

| What | ID pattern | Playwright selector |
|---|---|---|
| Table row | `object-row-{id}` | `#object-row-42` |
| Name cell | `object-row-{id}-name` | `#object-row-42-name` |
| Organisation cell | `object-row-{id}-organisation` | `#object-row-42-organisation` |
| Context menu button | `object-row-{id}-context-menu` | `#object-row-42-context-menu` |
| Page navigator | `page-navigator-{type}` | `#page-navigator-neubau` |

**Context menu option IDs (static — same for all rows):**
```
#object-context-menu-option-open-detail-view
#object-context-menu-option-open-questionnaire
#object-context-menu-option-handover-object
#object-context-menu-option-reject-object
```

---

## 4. Object Side Panel

**File:** `objectPanel.ids.ts`  
**Used by:** `usePanelIds()` hook → `SidePanel.tsx` root div  
**Note:** `usePanelIds()` reads the URL path to pick the correct ID set automatically.

### 4a. Neubau Object Panel

| What | ID | Attribute |
|---|---|---|
| Panel wrapper | `neubau-object-side-panel` | `id` |
| Panel wrapper aria | `Object side panel` | `aria-label` |
| Close button | `neubau-object-side-panel-close-button` | `id` |
| Go to Sales Actions | `neubau-object-go-to-sales-actions-button` | `id` |
| Status radio group | `neubau-object-status-radio-group` | `id` |
| Edit cancel button | `neubau-object-edit-cancel-button` | `id` |
| Edit apply button | `neubau-object-edit-apply-button` | `id` |
| Note — add | `neubau-object-note-add-button` | `id` |
| Note — edit | `neubau-object-note-edit-button` | `id` |
| Note — delete | `neubau-object-note-delete-button` | `id` |
| Note — cancel | `neubau-object-note-cancel-button` | `id` |
| Note — save | `neubau-object-note-save-button` | `id` |
| Note — send to A1 checkbox | `neubau-object-note-send-to-a1-checkbox` | `id` |
| Questionnaire edit | `neubau-object-questionnaire-edit-button` | `id` |
| Questionnaire cancel | `neubau-object-questionnaire-cancel-button` | `id` |
| Questionnaire save | `neubau-object-questionnaire-save-button` | `id` |
| D2D SA context menu (per row) | `neubau-door-sales-action-context-menu-button-{salesActionId}` | `id` |
| D2D SA detail view (per row) | `neubau-door-sales-action-detail-view-button-{salesActionId}` | `id` |
| D2D SA delete (per row) | `neubau-door-sales-action-delete-button-{salesActionId}` | `id` |

### 4b. FTTH Object Panel

| What | ID |
|---|---|
| Panel wrapper | `ftth-object-side-panel` |
| Close button | `ftth-object-side-panel-close-button` |
| Go to Sales Actions | `ftth-object-go-to-sales-actions-button` |
| Note — add | `ftth-object-note-add-button` |
| Note — edit | `ftth-object-note-edit-button` |
| Note — delete | `ftth-object-note-delete-button` |
| Note — cancel | `ftth-object-note-cancel-button` |
| Note — save | `ftth-object-note-save-button` |

### 4c. Bestandsbau Object Panel

| What | ID |
|---|---|
| Panel wrapper | `bestandsbau-object-side-panel` |
| Close button | `bestandsbau-object-side-panel-close-button` |
| Go to Sales Actions | `bestandsbau-object-go-to-sales-actions-button` |
| Note — add | `bestandsbau-object-note-add-button` |
| Note — edit | `bestandsbau-object-note-edit-button` |
| Note — delete | `bestandsbau-object-note-delete-button` |
| Note — cancel | `bestandsbau-object-note-cancel-button` |
| Note — save | `bestandsbau-object-note-save-button` |

---

## 5. Sales Actions — List Page

**File:** `salesAction.ids.ts`  
**Element:** `<tr>` and cells via `rowAttributes`

| What | ID pattern | Playwright selector |
|---|---|---|
| Table row | `sales-action-row-{id}` | `#sales-action-row-55` |
| Main info cell | `sales-action-row-{id}-main-info` | `#sales-action-row-55-main-info` |
| Status cell | `sales-action-row-{id}-status` | `#sales-action-row-55-status` |
| Context menu button | `sales-action-row-{id}-context-menu` | `#sales-action-row-55-context-menu` |
| Quick creation CI | `sales-action-row-{id}-quick-creation-customer-interaction` | `#sales-action-row-55-quick-creation-customer-interaction` |
| Pagination | `sales-actions-pagination` | `#sales-actions-pagination` |

**Context menu button aria-label (static):** `Open sales action context menu`

**Context menu option IDs (static):**
```
#sales-action-context-menu-option-open-detail-view
#sales-action-context-menu-option-record-activity
#sales-action-context-menu-option-record-sketch
#sales-action-context-menu-option-record-order
#sales-action-context-menu-option-customer-not-met
#sales-action-context-menu-option-not-reached
#sales-action-context-menu-option-delete-sales-action
```

---

## 6. Sales Action Side Panel

**File:** `salesActionPanel.ids.ts`  
**All IDs are dynamic — require the sales action DB id.**

| What | ID pattern |
|---|---|
| Edit status button | `sales-action-{id}-edit-status-button` |
| Edit assignment button | `sales-action-{id}-edit-assignment-button` |
| Note — add | `sales-action-{id}-note-add-button` |
| Note — edit | `sales-action-{id}-note-edit-button` |
| Note — delete | `sales-action-{id}-note-delete-button` |
| Hint — add | `sales-action-{id}-hint-add-button` |
| Hint — edit | `sales-action-{id}-hint-edit-button` |
| Hint — delete | `sales-action-{id}-hint-delete-button` |
| Hint — cancel | `sales-action-{id}-hint-cancel-button` |
| Hint — save | `sales-action-{id}-hint-save-button` |
| Capture activity button | `sales-action-{id}-capture-activity-button` |
| Delete button | `sales-action-{id}-delete-button` |
| Correct door number button | `sales-action-{id}-correct-door-number-button` |
| Plan sketch button | `sales-action-{id}-plan-sketch-button` |
| Capture order button | `sales-action-{id}-capture-order-button` |
| Capture Ableger button | `sales-action-{id}-capture-ableger-button` |
| Customer interaction toggle (per accordion) | `sales-action-{id}-customer-interaction-{num}-toggle` |
| Document item (per document) | `sales-action-{salesActionId}-document-{documentId}` |

**Status chip** — element has `role="status"` + `aria-label`:

| Status | `aria-label` value |
|---|---|
| Open | `open` |
| In Progress | `in progress` |
| Closed (negative) | `closed - negative` |
| Closed (positive) | `closed - positive` |
| Carried Out | `completed` |
| Not Executable | `not feasible` |

```ts
// Playwright selector for status chip
page.locator('[role="status"][aria-label="closed - positive"]')
page.locator('[role="status"][aria-label="in progress"]')
```

**Capture Activity Modal (static IDs):**
```
#sales-action-activity-modal
#sales-action-activity-cancel-button
#sales-action-activity-save-button
#activity-performed-by-option-{userId}   ← dynamic
```

---

## 7. User Administration — List Page

**File:** `userAdministration.ids.ts`

| What | ID pattern |
|---|---|
| Table row | `user-row-{id}` |
| First name cell | `user-{id}-first-name` |
| Last name cell | `user-{id}-last-name` |
| Status cell | `user-{id}-status` |
| Email cell | `user-{id}-email` |
| Organisation cell | `user-{id}-organization` |
| Role cell | `user-{id}-role` |
| Context menu button | `user-{id}-context-menu-button` |
| Page navigator | `page-navigator-users` |
| Create user button | `create-user-button` |
| Create team button | `create-team-button` |
| Create Admin A1 button | `create-admin-a1-button` |

**Context menu options (dynamic — include userId):**
```
#user-{id}-context-menu-option-details
#user-{id}-context-menu-option-edit
#user-{id}-context-menu-option-deactivate
#user-{id}-context-menu-option-delete
#user-{id}-context-menu-option-activate
```

---

## 8. User Side Panel

**File:** `userSidePanel.ids.ts`  
**Wrapper div attributes:**

| Attribute | Value |
|---|---|
| `id` | `user-side-panel-{userId}` |
| `data-user-id` | `"{userId}"` |
| `aria-label` | `"User details for {firstName} {lastName}"` |
| `data-panel-state` | `"open"` |

**Static IDs (not user-specific):**

| What | ID |
|---|---|
| Wrapper (static fallback) | `user-side-panel` |
| Close button (static) | `user-side-panel-close-button` |
| Add role button | `user-side-panel-add-role-button` |
| Activate — cancel | `activate-user-cancel-button` |
| Activate — confirm | `activate-user-confirm-button` |
| Deactivate — cancel | `deactivate-user-cancel-button` |
| Deactivate — confirm | `deactivate-user-confirm-button` |
| Delete — cancel | `delete-user-cancel-button` |
| Delete — confirm | `delete-user-confirm-button` |

**Dynamic IDs (require userId):**

| What | ID pattern |
|---|---|
| Wrapper | `user-side-panel-{id}` |
| Edit button | `user-{id}-edit-button` |
| Deactivate button | `user-{id}-deactivate-button` |
| Delete button | `user-{id}-delete-button` |
| Activate button | `user-{id}-activate-button` |
| Edit cancel | `user-{id}-edit-cancel-button` |
| Edit save | `user-{id}-edit-save-button` |
| Activate dialog | `activate-user-dialog-{id}` |
| Deactivate dialog | `deactivate-user-dialog-{id}` |
| Delete dialog | `delete-user-dialog-{id}` |
| Info — organisation | `user-{id}-organisation` |
| Info — corporate account | `user-{id}-corporate-account` |
| Info — partnerweb ID | `user-{id}-partnerweb-id` |
| Info — name | `user-{id}-name` |
| Info — email | `user-{id}-email` |
| Info — status | `user-{id}-status` |
| Info — created at | `user-{id}-created-at` |
| Info — created by | `user-{id}-created-by` |
| Info — updated at | `user-{id}-updated-at` |
| Info — updated by | `user-{id}-updated-by` |
| Role edit button | `user-side-panel-role-edit-button-{roleValue}` |
| Role delete button | `user-side-panel-role-delete-button-{roleValue}` |

---

## 9. Teams — List Page

**File:** `teams.ids.ts`

| What | ID pattern |
|---|---|
| Table row | `team-row-{id}` |
| Context menu button | `team-{id}-context-menu-button` |
| Context menu — details | `team-{id}-menu-details` |
| Context menu — edit | `team-{id}-menu-edit` |
| Context menu — delete | `team-{id}-menu-delete` |
| Pagination | `teams-pagination` |

---

## 10. Team Side Panel

**File:** `teamSidePanel.ids.ts`  
**Wrapper div attributes:**

| Attribute | Value |
|---|---|
| `id` | `team-side-panel-{teamId}` |
| `data-team-id` | `"{teamId}"` |
| `aria-label` | `"Team details for {teamName}"` |
| `data-panel-state` | `"open"` |

**Dynamic IDs (require teamId):**

| What | ID pattern |
|---|---|
| Wrapper | `team-side-panel-{id}` |
| Delete button | `team-{id}-delete-button` |
| Edit button | `team-{id}-edit-button` |
| Edit cancel | `team-{id}-edit-cancel-button` |
| Edit save | `team-{id}-edit-save-button` |
| Add user button | `team-{id}-add-user-button` |
| Add user cancel | `team-{id}-add-user-cancel-button` |
| Add user apply | `team-{id}-add-user-apply-button` |
| Agent checkbox | `team-{id}-agent-checkbox` |
| Channel checkbox | `team-{id}-channel-checkbox` |
| Remove member button | `team-{teamId}member{userId}-remove-button` |
| Delete dialog | `delete-team-dialog-{id}` |
| Info — organisation | `team-{id}-organisation` |
| Info — name | `team-{id}-name` |

**Static IDs:**
```
#delete-team-cancel-button
#delete-team-confirm-button
```

---

## 11. Organizations — List Page

**File:** `organizations.ids.ts`

| What | ID pattern |
|---|---|
| Table row | `organization-row-{id}` |
| Name cell | `organization-{id}-name` |

---

## 12. Organization Side Panel

**File:** `organizationSidePanel.ids.ts`  
**Wrapper div attributes:**

| Attribute | Value |
|---|---|
| `id` | `organization-side-panel-{orgId}` |
| `data-panel-state` | `"open"` |

| What | ID pattern |
|---|---|
| Wrapper | `organization-side-panel-{id}` |
| Close button | `organization-side-panel-{id}-close-button` |
| Info — name | `organization-{id}-name` |
| Info — email addresses | `organization-{id}-email-addresses` |
| Info — sales region | `organization-{id}-sales-region` |

---

## 13. Imports — List Page + Actions

**File:** `imports.ids.ts`

| What | ID pattern |
|---|---|
| Table row | `import-row-{id}` |
| Import column cell | `import-{id}-col-import` |
| Imported by | `import-{id}-imported-by` |
| Baulos column | `import-{id}-col-baulos` |
| Baulos name | `import-{id}-baulos-name` |
| Organisation column | `import-{id}-col-organisation` |
| Organisation name | `import-{id}-organisation-name` |
| Status tag | `import-{id}-status` |
| Revert button | `import-{id}-revert-button` |
| Pagination | `imports-pagination` |
| Revert modal | `import-{id}-revert-modal` |
| Revert modal — cancel | `import-{id}-revert-cancel-button` |
| Revert modal — delete | `import-{id}-revert-delete-button` |
| Revert modal — close | `import-{id}-revert-close-button` |

**Import Actions (static):**
```
#imports-change-organization-button
#change-organization-modal
#change-organization-cancel-button
#change-organization-confirm-button
#imports-import-data-button
#import-data-modal
#import-data-cancel-button
#imports-file-uploader
```

---

## 14. Configuration — Navigation

**File:** `configurationInteractionOutcomeList.ids.ts`

| What | ID | Attribute |
|---|---|---|
| Config nav sidebar | `configuration-navigation-sidebar` | `id` |
| Nav sidebar — tabs list | — | `data-nav-items="Tab1,Tab2,..."` (comma-separated) |

```ts
// Assert which config tabs are open
page.locator('#configuration-navigation-sidebar[data-nav-items*="Abschlussgründe"]')
```

---

## 15. Configuration — Abschlussgründe (Interaction Outcome)

**File:** `configurationInteractionOutcomeList.ids.ts`

**Sidebar attributes:**

| Attribute | Value |
|---|---|
| `id` | `interaction-outcome-sidebar` |
| `data-sidebar-state` | `"open"` or `"closed"` |

| What | ID pattern |
|---|---|
| Sidebar | `interaction-outcome-sidebar` |
| Table row | `interaction-outcome-row-{id}` |
| Context menu button | `interaction-outcome-{id}-context-menu-button` |
| Nav tab item | `interaction-outcome-nav-child-{id}` |
| Nav tab close button | `interaction-outcome-nav-child-{id}-close-button` |
| Detail child item | `interaction-outcome-child-{id}` |
| Detail child close button | `interaction-outcome-child-{id}-close-button` |

```ts
// Sidebar state check
page.locator('#interaction-outcome-sidebar[data-sidebar-state="open"]')
page.locator('#interaction-outcome-sidebar[data-sidebar-state="closed"]')
```

**Create modal (static IDs):**
```
#create-interaction-outcome-button
#create-interaction-outcome-modal
#create-interaction-outcome-close-button
#create-interaction-outcome-cancel-button
#create-interaction-outcome-confirm-button
```

---

## 16. Configuration — Aufgaben (Sales Action Tasks)

**File:** `configurationSalesActionTaskList.ids.ts`

| What | ID pattern |
|---|---|
| Table row | `task-row-{id}` |

**Create modal (static IDs):**
```
#create-task-button
#create-task-modal
#create-task-close-button
#create-task-cancel-button
#create-task-confirm-button
```

---

## 17. Configuration — Regime List

**File:** `configurationRegimeList.ids.ts`  
**Row `data-*` attributes set on `<tr>`:**

| Attribute | Source field |
|---|---|
| `data-regime-id` | `regime.id` |
| `data-object-type` | `regime.typeName` |
| `data-regime` | `regime.subTypeName` |
| `data-display-name` | `regime.subTypeName` |
| `data-sub-type` | `regime.subType` |
| `data-created-at` | formatted `regime.createdTimestamp` |

| What | ID pattern |
|---|---|
| Table row | `regime-row-{id}` |
| Context menu button | `regime-{id}-context-menu-button` |

```ts
// Scope context menu to a specific row
const row = page.locator('#regime-row-5')
await row.locator('#regime-5-context-menu-button').click()

// Filter by object type
page.locator('[data-object-type="NEUBAU"]')
```

**File:** `configurationRegimeListCreate.ids.ts`

```
#create-regime-button
#create-regime-modal
#create-regime-cancel-button
#create-regime-confirm-button
```

---

## 18. Configuration — Aktivitäten Setup

**File:** `configurationActivitySetupList.ids.ts`  
**Row `data-*` attributes set on `<tr>`:**

| Attribute | Source field |
|---|---|
| `data-activity-setup-id` | `is.id` |
| `data-object-type` | `is.objectType` |
| `data-regime` | `is.objectSubtype` |
| `data-task` | `is.taskName` |

| What | ID pattern |
|---|---|
| Table row | `activity-setup-row-{id}` |
| Context menu button | `activity-setup-{id}-context-menu-button` |
| Create button | `create-activity-setup-button` |

**Context menu button aria-label (static):** `Open activity setup actions`

```ts
// Row with all data attributes
page.locator('#activity-setup-row-3')
page.locator('[data-object-type="NEUBAU"][data-regime="Grundversorgung"]')

// Context menu
await page.locator('#activity-setup-3-context-menu-button').click()

// Create button
await page.locator('#create-activity-setup-button').click()
```

---

## Common Playwright Patterns

### Scoping assertions inside a panel
```ts
const userPanel = page.locator('#user-side-panel-42')
await userPanel.locator('#user-42-edit-button').click()
```

### Scoping assertions inside a modal
```ts
const modal = page.locator('#create-regime-modal')
await modal.locator('#create-regime-cancel-button').click()
await modal.locator('#create-regime-confirm-button').click()
```

### Checking panel open state
```ts
page.locator('#user-side-panel-42[data-panel-state="open"]')
page.locator('#team-side-panel-7[data-panel-state="open"]')
page.locator('#organization-side-panel-3[data-panel-state="open"]')
```

### Filtering table rows by data attribute
```ts
page.locator('tr[data-object-type="NEUBAU"]')
page.locator('tr[data-regime="Grundversorgung"]')
page.locator('tr[data-task="Türbegang"]')
```

### Quick filter assertion
```ts
await page.locator('#quick-filter-objectType-NEUBAU').click()
await expect(page.locator('#quick-filter-objectType-NEUBAU')).toHaveAttribute('aria-pressed', 'true')
```

### Status chip assertion (Sales Action panel)
```ts
await expect(page.locator('[role="status"]')).toHaveAttribute('aria-label', 'closed - positive')
```
